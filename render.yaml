services:
  - type: web
    name: english-tenses-trainer
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm run preview
    publishDir: dist
    envVars:
      - key: NODE_VERSION
        value: 20
