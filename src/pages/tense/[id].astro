---
import BaseLayout from '../../layouts/BaseLayout.astro';
import tensesData from '../../data/tenses.json';

export function getStaticPaths() {
  return Object.values(tensesData).map((tense) => ({
    params: { id: tense.id },
    props: { tense },
  }));
}

const { tense } = Astro.props;
---

<BaseLayout title={`${tense.nameRu} — ${tense.nameEn} | Tense Trainer`}>
  <link slot="head" rel="stylesheet" href="/src/styles/base.css" />
  
  <div class="app-container">
    <header class="header">
      <div class="header-top">
        <div>
          <div style="font-size:0.8rem;color:var(--primary-light);font-weight:600;margin-bottom:4px">
            <a href="/" style="color:inherit;text-decoration:none">🌐 временаванглийском.рф</a>
          </div>
          <h1>🎯 {tense.nameRu}</h1>
          <p>{tense.nameEn}</p>
        </div>
      </div>
    </header>

    <nav class="nav">
      <a href="/" class="nav-btn">← На главную</a>
    </nav>

    <article class="card">
      <h2 class="card-title">📋 Когда используется</h2>
      <ul style="margin-left: 20px; line-height: 2;">
        {tense.useCases.map((useCase) => (
          <li>{useCase}</li>
        ))}
      </ul>
    </article>

    <article class="card">
      <h2 class="card-title">📐 Формулы</h2>
      <div class="formula-box">
        <div class="formula-row">
          <span class="formula-label pos">+</span>
          <span>{tense.formulas.affirmative}</span>
        </div>
        <div class="formula-row">
          <span class="formula-label neg">−</span>
          <span>{tense.formulas.negative}</span>
        </div>
        <div class="formula-row">
          <span class="formula-label q">?</span>
          <span>{tense.formulas.question}</span>
        </div>
        <div class="formula-row" style="background:#fef3c7;border-radius:4px;padding:8px;">
          <span style="margin-right:8px;">👍/👎</span>
          <span>{tense.formulas.shortAnswers}</span>
        </div>
      </div>
    </article>

    <article class="card" style="background: #fef3c7;">
      <h2 class="card-title">🔔 Сигнальные слова</h2>
      <div>
        {tense.signalWords.map((word) => (
          <span class="signal-word">{word}</span>
        ))}
      </div>
    </article>

    <article class="card">
      <h2 class="card-title">📝 Примеры</h2>
      {tense.examples.map((example) => (
        <div class="example-box">
          <p class="example-en">{example.en}</p>
          <p class="example-ru">{example.ru}</p>
        </div>
      ))}
    </article>

    <footer>
      <p style="margin:0;font-size:0.875rem">© 2026 Tense Trainer</p>
    </footer>
  </div>
</BaseLayout>
